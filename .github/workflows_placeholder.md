# CI note

For the 48-hour local sprint, clean macOS builds are the priority. Add GitHub Actions after Step 2 if it helps the team; do not spend baseline-critical time on CI polish before the simulator compiles and tests locally.
